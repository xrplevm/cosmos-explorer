# Cosmos Explorer

The repository documentation has been consolidated into `memory/`.

Start here:

- `memory/repository_guide.md` for repository overview, commands, architecture, and verification
- `memory/agent_working_rules.md` for agent-specific working conventions
- `memory/MEMORY.md` for the memory index
