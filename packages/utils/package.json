{
  "name": "@cosmos-explorer/utils",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "tsc --project tsconfig.json --watch",
    "typecheck": "tsc --project tsconfig.typecheck.json",
    "clean": "rm -rf dist",
    "lint": "eslint ."
  },
  "dependencies": {
    "@cosmos-explorer/core": "workspace:*",
    "bech32": "^2.0.0"
  },
  "devDependencies": {
    "@cosmos-explorer/eslint-config": "workspace:^",
    "eslint": "^9",
    "typescript": "^5"
  }
}
