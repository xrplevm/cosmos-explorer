{
  "name": "@cosmos-explorer/price",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "../core/node_modules/.bin/tsc --project tsconfig.json",
    "dev": "../core/node_modules/.bin/tsc --project tsconfig.json --watch",
    "typecheck": "../core/node_modules/.bin/tsc --project tsconfig.typecheck.json",
    "clean": "rm -rf dist",
    "lint": "eslint ."
  },
  "dependencies": {
    "@cosmos-explorer/core": "workspace:*",
    "@cosmos-explorer/utils": "workspace:*"
  },
  "devDependencies": {
    "@cosmos-explorer/eslint-config": "workspace:^",
    "eslint": "^9",
    "typescript": "^5"
  }
}
